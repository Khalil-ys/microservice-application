spring:
  datasource:
    #    url: jdbc:h2:mem:db
    url: jdbc:h2:~/test
    username: sa
    password:
  jpa:
    defer-datasource-initialization: true
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: validate
    show-sql: true
  h2:
    console:
      enabled: true
